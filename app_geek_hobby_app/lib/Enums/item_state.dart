// for later use, to replace with all the toggles

enum GameState
{
    none,
    wishlist,
    backlog,
    inProgress,
    completed
}

enum ItemState // default state for items that don't have a specific state
{
    none,
    wishlist,
    backlog,
    inProgress,
    completed
}

enum AnimeState
{
    none,
    wishlist,
    inProgress,
    completed
}